# Server-side only. Used by `npm run metrics:refresh`; never prefix with VITE_.
YOUTUBE_API_KEY=YOUR_SERVER_SIDE_YOUTUBE_DATA_API_KEY

# Optional path, relative to the repository root. The checked-in default is used otherwise.
# YOUTUBE_CHANNEL_CONFIG=scripts/youtube-channels.json
